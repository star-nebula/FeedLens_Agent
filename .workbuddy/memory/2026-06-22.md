# 2026-06-22 工作日志

## 生成 ARCHITECTURE_FOR_RESUME.md

- 全面扫描项目代码（main_agent.py / state.py / ranking_agent.py / feedback_agent.py / database.py / push_scheduler.py / API.md）
- 生成 ARCHITECTURE_FOR_RESUME.md，约 1800 字，5 个维度：
  1. 顶层架构图（Orchestrator + Planner + Workers + Aggregator）
  2. 数据模型（11 张表 + 个性化筛选混合存储策略）
  3. 核心 API（4 个对外 + 4 个对内/监控接口）
  4. 技术决策（APScheduler选型 / 进程内State vs MQ / 向量排序双路偏好）
  5. 瓶颈分析（Router Node 单点 LLM 延迟 + 分层路由优化思路）

## 生成 FeedLens_Simple_Architecture2.drawio

- 对比原图（FeedLens_Simple_Architecture.drawio）与实际代码，识别 5 处差异
- 采用「Python 脚本写入法」绕过 Shell 特殊字符 + 命令行长度限制（CreateProcessW 32766 字符上限）
- 修正版路径：`docs/architecture/FeedLens_Simple_Architecture2.drawio`（35470 bytes）
- 5 处修正：
  1. 新增 `understand_intent` 为独立节点（生成 structured_goal + goal_embedding）
  2. 新增 `router_node` 节点（LLM 动态路由，黄色高亮标注 NEW）
  3. `coordinator_reflect` 作为独立节点（quality<0.7 回退 planner）
  4. Breaking News 破例触发路径细化（Ranking→push_immediate）
  5. FeedbackAgent 正确体现（feedback_bias + EMA 更新偏好向量）
- 图结构：5 层（输入层→大脑决策层 8 节点→子Agent 执行层→记忆系统层→输出层）
- **BugFix**: 修复 l5_title 的 mxGeometry 中重复属性 `width="300" width="25"` → `width="300" height="25"`，解决 draw.io 打开报错 `Cannot read properties of null (reading 'getAttribute')`

## 架构图持续优化

- 用户手动修补 drawio 后要求审查，发现 P0 级问题：`e_mem_read2` 缺少 `target="nd_planner"`，ChromaDB 读记忆箭头不连 planner
- `arr_fallback` 线原为悬空注释线（不连任何节点），改为从 `nd_router`→`nd_invoke` 的虚线分支，与 `e_rtr_inv`（LLM 决策实线）并行为两条并行边
- 用户质疑"管线 vs 自主性"，解释了主Agent自主决策层 vs 子Agent管线执行层的两层架构设计

## 生成 FeedLens_Autonomy_Architecture.drawio

- 新建 `docs/architecture/FeedLens_Autonomy_Architecture.drawio`，只画自主性相关部分
- 三层结构：触发自主（APScheduler+Breaking News）→ 决策自主（planner/router/reflect ReAct循环）→ 进化自主（FeedbackAgent EMA反馈闭环）
- 底部总结对比：vs 工作流 Agent 的固定 DAG，突出每次路由动态决策
