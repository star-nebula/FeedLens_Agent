# 2026-06-23 工作日志

## 生成 enrich_metadata 测试脚本文档

- 读取 `scripts/test_enrich_metadata.py` 完整源码（394 行）
- 生成 `docs/test.md`：包含脚本目的、用法、测试数据、两种模式验证标准、关键设计点、输出示例

## 向量预过滤跨批次去重优化规划

- 用户提出：能否用向量先分析每条数据在数据库中有没有，再丢给 LLM，这样会快很多
- 深入分析现有 dedup 架构：`fc_tools.py:335` 的 `deduplicate()` 只做批次内去重（O(n²)），不查历史 DB
- 量化瓶颈：62 条中 70%+ 可能是历史重复，但仍全部进入 Ranking Agent 的 LLM rank_items（占总耗时 60s+）
- 生成优化规划文档 `docs/Change&TODO/优化规划_向量预过滤跨批次去重.md`（~350 行）
- 方案核心：在 Collection→Ranking 之间插入 ChromaDB `feed_items` 向量查询，阈值 0.92 直接拦截明显重复
- 预期：进入 Ranking 条目减少 73%，总管线耗时缩短 60%（~95s→~38s），纯 CPU 预过滤仅 ~2s
- 更新 changelog.md v2.1.0 条目
